services:
  backend:
    build: .
    container_name: pantry_prod
    restart: always
    ports:
      - "80:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data # <--- ESTO GUARDA TU BASE DE DATOS EN EL DISCO DEL SERVIDOR